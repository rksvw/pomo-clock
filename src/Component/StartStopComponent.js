import React, { Component } from 'react'

export default class StartStopComponent extends Component {
  render() {
    return (
      <div>
        <button>Start</button>
        <button>Reset</button>
      </div>
    )
  }
}
